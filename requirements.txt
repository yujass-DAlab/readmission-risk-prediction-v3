# --- Serving layer ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# --- AWS SDK (S3 + SSM) ---
boto3>=1.34.0
botocore>=1.34.0

# --- Model runtime (needed to unpickle the trained model) ---
joblib>=1.3.0
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.4.0
xgboost>=2.0.0,<3.0
lightgbm>=4.0.0

# --- Training-only (uncomment if running train_pipeline_v3.py) ---
# pandas>=2.0.0
# imbalanced-learn>=0.12.0